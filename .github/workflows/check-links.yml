name: Check source links

on:
  schedule:
    - cron: "17 8 * * 1"
  workflow_dispatch:

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Request each registry URL
        run: python3 scripts/check_links.py
